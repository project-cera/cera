# GPU override for MDQA evaluation
# Usage: docker-compose -f docker-compose.yml -f docker-compose.gpu.yml up cli
services:
  cli:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
